{
  "name": "predge-arc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Predge settlement contract on Circle Arc (testnet) — Arc-native leg of Predge's keyless x402 pay-per-call model.",
  "scripts": {
    "genwallet": "node script/genwallet.js",
    "deploy": "node script/deploy.js"
  },
  "dependencies": {
    "ethers": "^6.13.4",
    "solc": "0.8.26"
  }
}
